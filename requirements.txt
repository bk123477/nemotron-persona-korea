# ────────────────────────────────────────────────────────────────────
# Nemotron-Personas-Korea 통합 requirements
# Python 3.14+ | 설치: .venv/bin/pip install -r requirements.txt
# ────────────────────────────────────────────────────────────────────

# ── 데이터 로딩 ───────────────────────────────────────────────────────
datasets>=2.0.0
pyarrow>=12.0.0
pandas>=2.0.0

# ── RAG 코어 ──────────────────────────────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0
rank-bm25>=0.2.2
openai>=1.0.0
httpx>=0.24.0

# ── Cohere 리랭커 ─────────────────────────────────────────────────────
cohere>=5.0.0

# ── LangChain ─────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-chroma>=0.1.0
langchain-text-splitters>=0.2.0

# ── LangGraph ─────────────────────────────────────────────────────────
langgraph>=0.1.0

# ── MCP (Model Context Protocol) ──────────────────────────────────────
mcp>=1.0.0

# ── 온톨로지 (Stage 5) ────────────────────────────────────────────────
rdflib>=6.0.0

# ── 유틸리티 ──────────────────────────────────────────────────────────
pydantic>=2.0.0
python-dotenv>=1.0.0

# ── 테스트 ────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
